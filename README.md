    - 1️⃣ What is the difference between var, let, and const?

    they all are used to declare a varibale but the difference between them is if i declare a variable with const i cannot upgrade the variable at all in the future but with var and let i can. Var is the old way of declaring variable. it hoist the varible but const and let do not hoist  there variables, they are blocked-scoped.

    - 2️⃣ What is the spread operator (...)?

    in simple language it expands an array or string into seperate element

    - 3️⃣ What is the difference between map(), filter(), and forEach()?

    foreach() apply the condition or the function to all of the element of an array without returning.

    map() does the same thing as array but it returns a new array.

    - 4️⃣ What is an arrow function?

    shortcut way of declaring an array. difference bewteen it and normal function is that arrow do hoist on the other hand the normal one is hoisted

    - 5️⃣ What are template literals?

    it is the string written in between ${} this. it is used to write code without multiline
